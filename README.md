# Writing a C compiler in Python



### private:
	- added bash script to run compiler. In order to start the compiler copy and run the following code:
		```
		chmod +x bin/run.sh; ./bin/run.sh
		```
	- added example programs